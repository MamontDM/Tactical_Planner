import React, { useEffect, useState } from 'react';

const ShipListCard =({ship}) => {
    
    const {shipData} = ship;


    useEffect(() => {
        if(ship);
    },[ship]);

    return null;
};


export default ShipListCard;